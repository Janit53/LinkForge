1. The main problem with stateful authentication in a microservices architecture is that the authentication state has to be shared or consistently available across multiple service instances.

2. JWT is mainly used in micro-services and uncentralised auth server.